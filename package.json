{
  "name": "@streamscloud/streamscloud-api",
  "version": "1.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "npm run build && npm version minor -f && package-version-git-tag --push"
  },
  "author": "defor",
  "license": "ISC",
  "files": [
    "dist"
  ],
  "dependencies": {
    "fs": "0.0.1-security",
    "node-fetch": "^2.6.0",
    "prettier": "^1.19.1"
  },
  "devDependencies": {
    "@types/node": "^13.5.3",
    "package-version-git-tag": "^3.0.0",
    "typescript": "^3.7.5"
  }
}
